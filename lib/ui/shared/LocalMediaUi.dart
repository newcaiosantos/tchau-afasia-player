import 'package:flutter/cupertino.dart';

class LocalMediaUi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Local media"),
    );
  }
}
